THERAPY TRACKER

This is a mobile-friendly PWA. It stores sessions locally in the browser on the device.

To use on iPhone: host these files on any HTTPS web host, open the site in Safari, tap Share -> Add to Home Screen -> turn on Open as Web App -> Add.

The app includes dashboard, add session, status, history and attendance percentage.
